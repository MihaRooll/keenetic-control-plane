# Router Control: инструкции для агентов

## Cold start

Перед любой работой прочитайте по порядку:

1. `README.md`
2. `docs/STATUS.yaml`
3. `docs/DEDICATED_ROUTER_LAB_POLICY.md` — **living SSOT** for lab class, expendable envelope, Gate A rebind (2026-07-31 ReadOnlyCertified)
4. `docs/CANONICAL.md`
5. `docs/contracts/README.md`
6. `docs/contracts/AI_HANDOFF.md` — cold-start extensions, SSOT, task template
7. `docs/SESSION_HANDOFF_REAL_ROUTER_2026-08-02.md` — **current** real-router session narrative (2026-08-01/02: Gate A parser-false-drift recert, guest AP3 live campaign, traps, honest status); **living policy SSOT** remains `DEDICATED_ROUTER_LAB_POLICY.md` + `STATUS.yaml`. Human gate packets (links only): `docs/HUMAN_GATE_GATE_A_IDENTITY_DRIFT_20260801.md`; KeenDNS cloud booking **APPROVED standing 2026-08-08** — `docs/HUMAN_GATE_KEENDNS_CLOUD_BOOKING_20260801.md` + `STATUS.yaml` `approvals.keendns_cloud_booking_expendable` (**do not re-ask**). Historical methods companion: `docs/SESSION_HANDOFF_REAL_ROUTER_2026-07-31.md`; prior-unit methods: `docs/SESSION_HANDOFF_REAL_ROUTER_2026-07-24.md`
   - **Recommended (methodology):** `docs/ENGINEERING_LESSONS.md` — transferable lab judgement, offline-reliability traps, and agent-delegation lessons (L-1..L-20, D-1..D-5); companion to handoff assumption traps; does **not** override POLICY/STATUS
   - **Required (main screen SSOT):** `docs/OPERATOR_SIMPLE_MAIN_MENU_SPEC.md` — operator requirement list for the recomposed main menu (living SSOT; device-verified capabilities per `.cursor/plans/main-decisions-local-hub.md` §M-24..§M-46); staff/guest R-3..R-6 blocked behind [`docs/HUMAN_GATE_STAFF_GUEST_MAIN_MENU_20260805.md`](docs/HUMAN_GATE_STAFF_GUEST_MAIN_MENU_20260805.md)
   - **Recommended (UI):** `docs/OPERATOR_UI.md`, `docs/OPERATOR_ROUTER_CONFIG_UI.md` — prototype UI runbooks; **`docs/OPERATOR_WEB_UI_FULL_COVERAGE_PLAN.md`** — deferred full-coverage plan, **not** current `next_task` (`local-hub-vpn-real-peer-autoconnect-continuation` per `STATUS.yaml`); `docs/contracts/ROADMAP.md` §3.3 for roadmap context
8. Task-specific contracts as needed (e.g. `docs/OPERATOR_NETWORK_FAMILY_APPLY_SCAFFOLD.md` for VLAN/DHCP/DNS/firewall preview scaffold; `docs/OPERATOR_VPN_CONNECTION_POLICY_DISCOVERY.md` for VPN policy-routing — still relevant for kill-switch/named-policy open questions)
9. `docs/project-state.md` (non-competing harness projection)

**New chat / current truth:** read `docs/STATUS.yaml` `next_task` first (`local-hub-vpn-real-peer-autoconnect-continuation`). Historical evening handoff (2026-08-07 Overview card-grid polish — **closeout done** in `STATUS.yaml` `day_10_evening_*`; do **not** re-run its §1 unfinished checklist): [`docs/NEW_CHAT_ORCHESTRATOR_PROMPT_2026-08-07-EVENING.md`](docs/NEW_CHAT_ORCHESTRATOR_PROMPT_2026-08-07-EVENING.md). Same-day day prompt: [`docs/NEW_CHAT_ORCHESTRATOR_PROMPT_2026-08-07.md`](docs/NEW_CHAT_ORCHESTRATOR_PROMPT_2026-08-07.md). Decisions companion: `.cursor/plans/main-decisions-local-hub.md` §M-53..§M-60. Staff/guest gate historical (live 2026-08-06): [`docs/HUMAN_GATE_STAFF_GUEST_MAIN_MENU_20260805.md`](docs/HUMAN_GATE_STAFF_GUEST_MAIN_MENU_20260805.md). Integration readiness crosswalk (honest — `module_3.0` not in this workspace, M7 unauthorized): [`docs/OPERATOR_HUB_MODULE_INTEGRATION_READINESS.md`](docs/OPERATOR_HUB_MODULE_INTEGRATION_READINESS.md). **Browser MCP (`cursor-ide-browser`) is usable** for UI verification, but registration is per-session and can vanish between chats — verify with a real tool call, not the server list (§M-41, §M-49). Older prompts: [`docs/NEW_CHAT_ORCHESTRATOR_PROMPT_2026-08-06.md`](docs/NEW_CHAT_ORCHESTRATOR_PROMPT_2026-08-06.md) … [`docs/NEW_CHAT_ORCHESTRATOR_PROMPT_2026-08-02.md`](docs/NEW_CHAT_ORCHESTRATOR_PROMPT_2026-08-02.md).

## Обязательные правила

- Соблюдайте текущую phase из `docs/STATUS.yaml`; не начинайте deliverables следующей phase заранее.
- **Dedicated lab router:** comprehensive hardware validation on the project-owned dedicated development router is **expected and in scope** ([`docs/DEDICATED_ROUTER_LAB_POLICY.md`](docs/DEDICATED_ROUTER_LAB_POLICY.md), [`docs/STATUS.yaml`](docs/STATUS.yaml) `approvals.dedicated_development_router_lab`). **`lab_class: expendable_development_router`** (authorized **2026-07-30**) marks the current throwaway test unit — factory-reset acceptable; no production traffic. **Gate A ReadOnlyCertified:** authorized rebinds **2026-07-31** (physical replacement + post-WG identity drift); six manual same-tuple freshness recerts **2026-08-01..2026-08-06** (**NOT rebinds**) before recertification was automated. **2026-08-07: same-tuple freshness recertification is now AUTOMATED** (§M-53/§M-54, [`docs/OPERATOR_GATE_A.md`](docs/OPERATOR_GATE_A.md) §20) — `scripts/recertify-gate-a-freshness.py` runs on a Windows Scheduled Task (`RouterControl-GateA-FreshnessAuto-Interval` every 4h + `-Logon` fallback every ≤30min) and rewrites `docs/gate-a-certification.json` **only** on a byte-identical tuple match (fail-closed on any real drift — silent rebind remains forbidden and unaffected); a background `gate_a_refresh_watchdog` inside the live host process reloads that file every ~2 minutes so an already-running host also self-heals without a restart. Check the CURRENT pointer in `docs/gate-a-certification.json` (`evidence_recorded_at`/`evidence_path`) rather than trusting a date written here — it changes automatically; host-key pin SHA256:RUi/peC9rUzYMT/CIgeIsBYjR5CFqYxxnCuUmfv2WkY has NOT changed since the 2026-07-31 rebind. Human gate packets: [`docs/HUMAN_GATE_GATE_A_IDENTITY_DRIFT_20260801.md`](docs/HUMAN_GATE_GATE_A_IDENTITY_DRIFT_20260801.md); KeenDNS/CrazeDNS cloud booking **APPROVED standing 2026-08-08** ([`docs/HUMAN_GATE_KEENDNS_CLOUD_BOOKING_20260801.md`](docs/HUMAN_GATE_KEENDNS_CLOUD_BOOKING_20260801.md) — agents **must not re-ask**; template `promo`/`netcraze.pro`/`auto`); [`docs/HUMAN_GATE_STAFF_GUEST_MAIN_MENU_20260805.md`](docs/HUMAN_GATE_STAFF_GUEST_MAIN_MENU_20260805.md) (historical — R-3..R-6 already live). **Device-verified capabilities (2026-08-05, expendable lab):** first WireGuard handshake (root cause: missing `PersistentKeepalive` on profile path); interface Address + `wireguard_ip_global`; client traffic via tunnel reversibly (higher `ip global` wins); station allowlist 14 entries + 7/7 live; dual-stack AllowedIPs import (IPv4 kept, IPv6 soft-drop); MSS apply/clear; VPN catalog live status + remove; main menu recomposed; VPN three-state UI + routing cross-check live; browser MCP usable for UI verification (§M-41) — **honesty:** MSS ≠ captive portal; Wi‑Fi auto-reconnect watchdog **not** live-proven; guest entry pages need additional HW; animation polish (R-10) not human-accepted; rockblack peer no reply; kill-switch/named policy **not** built; R-3..R-6 **blocked** behind human gate (not «КОД ГОТОВ»); test-DB isolation landed + adversarial-passed but **Main must confirm** before migration-16 resume. **Expendable autonomous envelope:** save, reboot, component/package install, firmware update/downgrade, factory reset, bounded Wi‑Fi APs (`WifiMaster0/1` + `AccessPoint0`–`6` per observed hardware inventory), **all** WireGuard interfaces, SSH/management enable, **KeenDNS/CrazeDNS cloud book/drop/update** (standing 2026-08-08) — no per-action confirmation when `ROUTER_CONTROL_LAB_CLASS=expendable_development_router` and live device matches recorded tuple. **Non-expendable standing auth (2026-07-24):** bounded reversible verification (exact tuple + host-key preflight; `Wireguard5`–`9`; `WifiMaster0/AccessPoint3`–`6`; backup; no save; `credential_ref`; evidence) — carve-outs require explicit per-action confirmation. **Absolute:** no secrets in repo; evidence honesty; do **not** open B/C/D; do **not** claim WriteCertified; do **not** set `write_shapes_registered=true`; **silent Gate A rebind forbidden**.
- Этот репозиторий — canonical project/prototype home. `ScanCursorIP` — только legacy behavioral evidence.
- Не утверждайте, что implementation, package, API или UI существуют, пока они фактически не созданы.
- Не выполняйте writes на реальном роутере вне applicable standing envelope ([`docs/DEDICATED_ROUTER_LAB_POLICY.md`](docs/DEDICATED_ROUTER_LAB_POLICY.md) §1a expendable / § standing non-expendable) или без явно открытого hardware mutation gate для точного identity/firmware/capability tuple; **live device ≠ recorded tuple → live writes fail-closed**; non-expendable carve-out writes всегда требуют explicit per-action human confirmation.
- Не добавляйте passwords, private keys, preshared keys, raw sessions, startup-config, credentials или другие secrets в код, документацию, fixtures, logs и artifacts.
- Unknown identity, firmware, capability или profile field означает fail-closed для writes.
- Не создавайте commit и не выполняйте push, если пользователь явно этого не запросил.
- Не изменяйте `.git` internals.
- Общайтесь с пользователем по-русски: все ответы в чате, объяснения и финальные отчёты — на русском языке; технические артефакты (код, команды, пути, английские SSOT-доки) не переводить.

<!-- cursor-project-toolkit-harness -->

## Toolkit harness (papercuts / patterns)

Bootstrapped from **cursor-project-toolkit** (product Essential). Project rules above still win.

- Product harness rules: `.cursor/rules/product-core.mdc` (with `project-docs-lifecycle.mdc` for doc changes).
- Living docs index: `docs/docs-map.json`; schema in `docs/docs-map-schema.md`; update via `/maintain-project-docs`. New-chat orchestrator prompt: `docs/NEW_CHAT_ORCHESTRATOR_PROMPT_2026-08-06.md` — maintain when project state shifts.
- Session projection: `docs/project-state.md` (non-competing with `docs/STATUS.yaml` SSOT).
- Failed shells may auto-log to `.papercuts.jsonl` via `.cursor/hooks`; details in `docs/papercuts.md`.
- Manual: `scripts/papercuts.ps1 add "<friction + fix>" -Tag tooling` (Windows: `$env:HOME = $env:USERPROFILE` if needed).
- Review: `/review-papercuts` or `papercuts list --format md`.
- Patterns: `prompting/`, `roles/`, `subagents/` when relevant.
- Change/build/fix: apply `.cursor/skills/autonomous-task` automatically; T0–T3 proceed autonomously, T4/destructive/external writes stop for human approval.
- Optional: `/add-plugin cursor-team-kit`; local `cursor-project-harness` installs via toolkit `scripts/install-harness-plugin.ps1`, not `/add-plugin`.

<!-- /cursor-project-toolkit-harness -->
